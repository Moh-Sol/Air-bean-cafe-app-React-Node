import React from 'react';
import Nav from '../Nav/Nav'
function Card(props) {
    return (
        <div>
            <Nav/>
            <h1>Din beställning</h1>
        </div>
    );
}

export default Card;